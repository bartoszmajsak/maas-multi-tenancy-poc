# Shared Models

Models deployed to `shared-models` namespace, accessible by multiple tenants.

## Resources

- `model.yaml` - LLMInferenceService definitions for shared models
- `rbac.yaml` - RBAC allowing tenants with different tiers to access models
